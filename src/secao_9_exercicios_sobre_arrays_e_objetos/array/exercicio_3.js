// * ATIVIDADE *
// crie um, array com 5 nome, incluindo o seu;
// verifique se o seu nome existe na array;
// se existir imprima alguma mensagem no console

let nomes = ["Lucas", "Pedro", "Anderson", "Jonas", "Gabriel"]

if (nomes.includes("Anderson")) {
    console.log( "Seu nome esta no array")
}else {
    console.log("Seu nome não esta no array")
}